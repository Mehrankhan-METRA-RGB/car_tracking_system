enum Filters { course, name, cnic }

enum Contact { email, phone, sms }
enum ImagePickType { passPort, idCard }
enum HistoryType { added, updated, deleted, payment }
enum DashboardTypeEnum {
  managementAccounts,
  registration,
  accountSection,
  courses,
  references,
}
enum CRUDEnum{
  read,write,delete,update
}
enum CookiesKeys{adminUserName,adminPassword,id}